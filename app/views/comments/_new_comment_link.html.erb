<% if logged_in? %>
  <%= link_to t("defaults.message.new_comment"), new_post_comment_path(post), data: { turbo_stream: true }, class: "btn btn-success rounded-pill mb-4" %>
<% else %>
  <%= link_to t("defaults.message.new_comment"), not_loggedin_comment_post_comments_path(post), class: "btn btn-success rounded-pill mb-4" %>
<% end %>
