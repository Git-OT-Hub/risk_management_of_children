<turbo-frame>
  <turbo-stream action="update" target="page-title">
    <% content_for(:title, "") %>
  </turbo-stream>
</turbo-frame>
<div class="container mt-5 mb-5">
  <div class="row">
    <div class="col col-lg-10 offset-lg-1">
      <div class="container text-center mb-5">
        <%= image_tag "test_video1.gif", size: "300x200" %>
      </div>
      <div class="container bg-body shadow-lg rounded p-3">
        <span class="top-page-font">「アプリ名」</span><br><br>
        「アプリ名」は、<span class="top-page-font">診断</span> により、自宅内における子どもにとっての危険な物や場所、状態を確認し、
        グッズを活用した安全対策を紹介するサービスです。<br><br>
        自宅内の環境は、各ご家庭によって様々なため、安全対策方法や使用するグッズ等が異なると思います。<br>
        そのため、<span class="top-page-font">投稿機能</span> においては、各ご家庭の環境に応じた安全対策方法を共有することで、助け合いの場になれば幸いです。
      </div>
    </div>
  </div>
</div>
<!-- テスト領域 -->
<div class="container mt-5 mb-5">
  <div class="row">
    <div class="col-12">
      <ul class="list-group">
        <% @diagnosis_contents.each do |content| %>
          <li class="list-group-item"><%= link_to content.title, diagnosis_content_path(content) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
