<%= link_to bookmark_path(post.bookmarks.find { |b| b.user_id == current_user.id }), data: { turbo_method: :delete, turbo_confirm: t("defaults.message.delete_bookmark") }, class:"float-end" do %>
  <i class="fa-solid fa-bookmark fa-xl"></i>
<% end %>
