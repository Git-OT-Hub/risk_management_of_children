<%= turbo_stream.prepend dom_id(@post, :comments), partial: "comment", locals: { comment: @comment } %>

<%= turbo_stream.update "comment_change_form", partial: "comment_control_panel_part", locals: { post: @post } %>

<%= turbo_stream_flash %>
